import { Button } from "@mui/material";

const RegisterBtn = () => {
   return (
      <Button variant="outlined" color="error">
         Register
      </Button>
   );
};

export default RegisterBtn;
